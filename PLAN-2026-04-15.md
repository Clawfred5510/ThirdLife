# ThirdLife — Actionable Plan (2026-04-15)

> **Master checklist lives in SuSuStudio** at [`docs/FULL-CHECKLIST.md`](https://github.com/Clawfred5510/SuSuStudio/blob/master/docs/FULL-CHECKLIST.md). This file is a mirror for convenience. When updating progress, edit **both** files and push.

## Context

The current build is broken (`@gamestu/shared` doesn't emit `dist/`, so typecheck + build fail). The domain `thirdlife.xyz` is a GoDaddy parking page — nothing is deployed. The existing `packages/dashboard` is an agent-ops console, not a way to play the game, so there is currently no reliable path for the owner to actually test what the bot has built.

The bot has also implemented jobs, NPCs, tutorials, and partial economy systems that are **not wanted for v1**. v1 is deliberately minimal: a city grid of parcels where players claim a parcel and place a business on it. Nothing else.

A sibling site at `thirdlifeworld.xyz` reportedly already has a parcel-claim system whose rules should be mirrored for claim pricing (confirm exact rules before Phase C implementation).

## Decisions

| Decision | Choice |
|---|---|
| Server host | **Railway** |
| Client host | **Cloudflare Pages** |
| Grid size | **50 × 50 = 2,500 parcels** |
| Claim cost | **Credits-based** — mirror `thirdlifeworld.xyz` (interim: 100 credits; 500 starting stipend) |
| Legacy systems | **Feature-flag off**, don't delete |

---

## Phase A — Unblock & Baseline

- [ ] A1a. Remove `"composite": true` from `packages/shared/tsconfig.json`
- [ ] A1b. Root `package.json`: build `shared` before `client`/`server`
- [ ] A1c. Verify `packages/shared/dist/index.js` + `index.d.ts` exist
- [ ] A2. Type `(stop, i)` callback in `packages/client/src/ui/components/FastTravel.tsx:63`
- [ ] A3. Green: `npm run typecheck && npm run build && npm run lint`
- [ ] A4a. Add root `npm run play` script (concurrently: server + client + browser)
- [ ] A4b. Update `README.md` — remove three-port instructions

## Phase B — Strip to the Core Loop

- [ ] B1. Create `packages/shared/src/features.ts` (`FEATURE_JOBS|NPCS|TUTORIAL|DAY_NIGHT`)
- [ ] B2a. Server: gate `spawnNPCs`, tutorial init, job handlers
- [ ] B2b. Client: hide Job Board, NPCs, Tutorial overlay when off
- [ ] B3a. Rewrite `packages/client/src/game/entities/buildings.ts` with `generateParcelGrid(50, 50)` (40 × 40-unit parcels)
- [ ] B3b. Roads between parcels (~8-unit width)
- [ ] B3c. Simplify `packages/client/src/game/scenes/MainScene.ts` — remove districts/river/bay/boulevards
- [ ] B3d. Drop landmark buildings for v1

## Phase C — Parcels & Businesses

- [ ] C1a. Drop `properties` table in `packages/server/src/db/index.ts`
- [ ] C1b. Create `parcels` table (id, grid_x, grid_y, owner_id, business_name, business_type, color, height, claimed_at)
- [ ] C1c. Seed 2,500 empty rows on first boot
- [ ] C2a. `claim_parcel` Colyseus handler (verify unclaimed, deduct credits, set owner, broadcast)
- [ ] C2b. `update_business` handler (verify owner, update, broadcast)
- [ ] C2c. `parcels` map in room state schema
- [ ] C3a. NEW `packages/client/src/ui/components/ParcelPanel.tsx` (claim / edit / read-only)
- [ ] C3b. Parcel click (raycast → id → panel)
- [ ] C3c. Minimap: claimed vs unclaimed
- [ ] C4a. Empty parcel: tinted ground square
- [ ] C4b. Owned parcel: colored Box at `color`/`height` + label
- [ ] C4c. Re-render on Colyseus state updates
- [ ] C5a. Claim → refresh → persists
- [ ] C5b. Restart server → persists
- [ ] C5c. Second client sees all parcels
- [ ] Confirm exact claim-cost rules with owner

## Phase D — Deploy

- [ ] D1a. Root `Dockerfile` (Node 20-slim, build shared+server)
- [ ] D1b. NEW `packages/server/src/config.ts` (env reader)
- [ ] D1c. Bind Colyseus to `0.0.0.0:$PORT`
- [ ] D1d. Verify `GET /health`
- [ ] D2a. `railway init` + `railway up`
- [ ] D2b. Volume `/data`; `DATABASE_PATH=/data/thirdlife.db`
- [ ] D2c. Env: `CLIENT_ORIGIN=https://thirdlife.xyz`, `FEATURE_*` off
- [ ] D2d. WebSockets on; optional `server.thirdlife.xyz`
- [ ] D3a. `packages/client/src/network/Client.ts`: `VITE_SERVER_URL` fallback
- [ ] D3b. `packages/client/.env.production` → `VITE_SERVER_URL=wss://server.thirdlife.xyz`
- [ ] D4a. Connect Cloudflare Pages
- [ ] D4b. Build: `npm -w packages/shared run build && npm -w packages/client run build`; output `packages/client/dist`
- [ ] D4c. Custom domain `thirdlife.xyz` (remove GoDaddy parking)
- [ ] D4d. `VITE_SERVER_URL` in Pages env
- [ ] D5. Trim `.github/workflows/deploy.yml` to CI checks
- [ ] D6a. External-network smoke test: spawn, move, chat
- [ ] D6b. Two-client sync test on prod
- [ ] D6c. Claim parcel on prod → reload → persists

## Phase E — Quality of Life

- [ ] E1. Player nameplates
- [ ] E2. Verify chat works post-cleanup
- [ ] E3. `/studio` admin page on server (minimal HTML, password-gated): list parcels, wipe world, ban player
- [ ] E4. `/metrics` endpoint (player count, claim count)

---

## Cleanup (tracked in SuSuStudio master)

- [ ] Delete `packages/dashboard/` — moved to SuSuStudio
- [ ] Update this repo's `README.md` to link SuSuStudio dashboard URL
- [ ] Rename `@gamestu/shared` npm scope (consistency)

## Open items

1. Claim-cost rules from `thirdlifeworld.xyz`
2. Server subdomain choice (`server.thirdlife.xyz` vs Railway URL)

## Critical files

| Phase | File | Change |
|---|---|---|
| A1 | `packages/shared/tsconfig.json` | Remove `composite` |
| A1 | root `package.json` | Ordered build |
| A2 | `packages/client/src/ui/components/FastTravel.tsx:63` | Types |
| A4 | root `package.json` | `play` script |
| B1 | `packages/shared/src/features.ts` | **NEW** |
| B2 | `packages/server/src/index.ts`, `packages/client/src/game/scenes/MainScene.ts` | Gate flags |
| B3 | `packages/client/src/game/entities/buildings.ts` | `generateParcelGrid` |
| B3 | `packages/client/src/game/scenes/MainScene.ts` | Flat world + grid |
| C1 | `packages/server/src/db/index.ts` | `parcels` table |
| C2 | `packages/server/src/rooms/GameRoom.ts` | Handlers + state |
| C3 | `packages/client/src/ui/components/ParcelPanel.tsx` | **NEW** |
| D1 | `Dockerfile`, `packages/server/src/config.ts` | **NEW** |
| D3 | `packages/client/src/network/Client.ts` | Env fallback |
